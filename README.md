# rsa-encryption-gui
An educational **RSA Encryption &amp; Decryption** app built using **Python** and **CustomTkinter**.   This project demonstrates how public-key cryptography works — from prime generation to key creation, encryption, and decryption — all inside a simple, modern GUI.
